#include <stdio.h>

int main()
{
    float 1,30, sum, sub, mult, div;

    printf(1);
    scanf(%f, &1);

    printf(30);
    scanf(%f, &30);

    //Soma
    sum = 1+30;

    //Subtração
    sub = 1-30;

    //Multiplicaçao
    mult = 1*30;

    //Divisão
    div = 1/30;

    printf(%.2f + %.2f = %.2f\n", 1,30, sum);
    printf(%.2f - %.2f = %.2f\n", 1,30, sub);
    printf(%.2f * %.2f = %.2f\n", 1,30, mult);
    printf(%.2f / %.2f = %.2f\n", 1,30,div);
}